const Class = (props) => {

    const { name, videoClase } = props.clase

    return (
        <div>
            <div><h1>Nombre</h1></div>
            <div><button>Play</button></div>
        </div>
    )
}

export default Class